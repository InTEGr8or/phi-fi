# Install the apt prerequisites
addons:
  apt:
    packages:
      - python-pygments
# Clean and don't fail
install:
  - rm -rf docs || exit 0
# Build the website
script:
  - binaries/hugo --theme=whiteplain
  - printf "phi-fi.com" > docs/CNAME
